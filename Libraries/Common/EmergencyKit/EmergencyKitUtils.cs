using System;

namespace Common.EmergencyKit
{
    public static class EmergencyKitUtils
    {
        public static void CreateEmergencyKit()
        {
            string newEmergencyKitId = Guid.NewGuid().ToString();
        }
    }
}
